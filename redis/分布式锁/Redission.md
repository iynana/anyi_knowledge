# 可重入原理
+ 利用**HASH结构**，记录获取**锁的线程**和**获取锁的次数**
	+ 第一个获取锁的线程设置锁，后续线程+1
	+ 最后一个释放锁的线程删除锁

# 看门狗机制
+ TTL不好设置，到期之后可能会多线程并行，设置自动续约。默认30s，每隔10s检查一次

# 主从模式下主节点宕机
**主从问题**
+ 此时如果从节点没有同步锁，可能会有线程安全问题

**解决方案**：
+ 此时需要部署多个 Redis 主节点，多主多从架构，向多个主节点都去获取锁
+ RedLock 红锁方案：要求 半数主节点以上（如 3/5）加锁成功，否则立即失败并释放已获取的锁。
