**定义**：大Key是指 Value体积过大（如==String类型 > 2MB，Hash/List元素>5000个==）的Key

**影响**：
1. 对大Key的操作 导致主线程的操作阻塞和资源消耗过高。
2. ==若使用RDB持久化，COW可能内存峰值升高，导致OOM==

**解决方案**：
1. **数据拆分**：
	- **Hash/List拆分**：将大Key拆分为多个小Key，例如将一个Hash Key拆分成多个Hash Key
2. **异步删除**：
	- **命令替换**：使用`UNLINK`（非阻塞）替代`DEL`，后台异步线程删除，避免影响主线程。