# ANN近似最近邻
向量数据库的检索基于ANN算法构建索引，将大量向量按索引分成几类。查询时先找到最相似的索引，然后再索引中精确匹配。

## HNSW算法
类似于跳表。
构建上下多层图结构，高层级由较少节点组成（快速定位），中层级包含更多节点（支持局部搜索），底层级包括所有数据点（定位最终搜索结果）

## IVF算法
首先通过聚类将所有向量分成多个“桶”(`nlist`)，查询时，先找到最相似的几个“桶”，然后只在这几个桶内进行精确搜索。`IVF_FLAT`、`IVF_SQ8`、`IVF_PQ` 是其不同变体，主要区别在于是否对桶内向量进行了压缩（量化）。