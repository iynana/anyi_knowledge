Skill 是==把重复性的工作流程打包成可复用模块的机制==。每个 Skill 是一个文件夹，里面有一份 SKILL.md 指令文件，还可以带上脚本、参考文档等资源。它和普通 prompt 最大的区别是：Skill 能被 Agent 自动发现和按需加载，不用你每次手动输入；和 MCP 工具的区别是：MCP 给 Agent ==提供外部工具和数据的访问能力==，而 Skill 教 Agent ==拿到这些工具和数据之后该怎么用==。

**核心**：把那些你反复在用的指令、流程、模板，打包成一个标准化的模块，Agent 自己知道什么时候该用它、怎么用它，不再依赖你手动复制粘贴。

**Skill结构**：
![[Pasted image 20260519135239.png]]

SKILL.md 的内容分两部分。顶部是一段 YAML 格式的**元数据**，声明这个 Skill 的名字和一句话描述；下面是**正文**，用 Markdown 写具体的指令和步骤。