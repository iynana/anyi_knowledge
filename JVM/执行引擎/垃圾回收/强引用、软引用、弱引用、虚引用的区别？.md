- 强引用：**只要所有 GC Roots 能找到，就不会被回收**
- 软引用：需要配合 SoftReference 使用，当垃圾多次回收，**内存依然不够**的时候会回收软引用对象
- 弱引用：需要配合 WeakReference 使用，**只要垃圾回收时，弱引用可达，并且没有同指向的强引用**，就会优先回收弱引用对象
- 虚引用：虚引用用于跟踪对象被垃圾回收的时机，必须配合**引用队列**使用，在对象被回收后接收**通知**，从而执行资源清理操作