**“孤儿进程”和“僵尸进程”是操作系统中两个常见的进程状态**

**孤儿进程**：孤儿进程指的就是一个进程的父进程已经退出或者终止了，但是这个子进程本身还在运行的情况。
+ 在 Unix/Linux 系统中，有一个 init 进程，进程号为1，如果一个进程变成了孤儿进程，那么操作系统会把他的父进程重新指定为 init 进程。

**僵尸进程**：子进程退出，父进程没有来得及处理子进程退出的信号，就会变成僵尸进程。僵尸进程占用的是进程号和一些资源，但它不再消耗CPU。
**如果系统中有大量的僵尸进程，可能会导致资源的浪费，因为这些进程仍然占据着有限的进程号。**