一个 Java 持久层框架（ORM框架），用于简化 Java 操作数据库的过程。

**优势**：
+ SQL 与代码解耦，灵活可控：MyBatis 允许开发者直接编写和优化 SQL
+ 动态 SQL 的强大支持：比如可以动态拼接SQL，通过 \<if\>, \<foreach\> 等标签动态生成 SQL，避免 Java 代码中繁琐的字符串拼接
+ 自动映射与自定义映射结合：自动将查询结果字段名与对象属性名匹配（如驼峰转换）
+ 插件扩展机制：可编写插件拦截 SQL 执行过程，实现分页、性能监控、SQL 改写等通用逻辑