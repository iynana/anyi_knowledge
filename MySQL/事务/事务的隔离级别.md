1. **读未提交**：一个事务还没提交时，它做的变更就能被别的事务看到
	+ 无需实现
	+ 会出现脏读、不可重复读、幻读的问题

2. **读已提交**：一个事务提交之后，它做的变更才会被其他事务看到
	+ MVCC
	+ 会出现不可重复读、幻读的问题
	**为什么选择用RC？**
	+ 提高并发（没有间隙锁）
	+ 减少死锁（RR 事务隔离级别会增加 Gap Lock 和 Next-Key Lock，这就使得锁的粒度变大，同时会使得死锁的概率增大）

3. **可重复读**：在一个事务中前后两次读取同一条数据，数据内容一致。是MySQL InnoDB 引擎的默认隔离级别
	+ MVCC
	+ 按SQL标准来说会出现幻读，但MySQL由于加入了==间隙锁的实现，也不会出现幻读==

4. **串行化**：对于同一行记录，“写”会加“写锁”，“读”会加“读锁”。当出现读写锁冲突的时候，后访问的事务必须等前一个事务执行完成，才能继续执行
	+ 加锁实现
	+ 问题都不会出现