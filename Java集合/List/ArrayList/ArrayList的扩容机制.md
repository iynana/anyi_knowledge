ArrayList 底层基于动态数组实现，默认初始容量为10，扩容操作代价较高，因此在已知元素数量时可预先指定容量。当调用 add 方法且当前数组已满时，会触发扩容：
- 计算新容量：通常为旧容量的1.5倍（oldCapacity + (oldCapacity >> 1)）。
- 创建新数组，并将原数组所有元素通过 Arrays.copyOf 复制到新数组。
- 将新数组赋值给内部数组引用，后续添加操作在新数组上进行。
