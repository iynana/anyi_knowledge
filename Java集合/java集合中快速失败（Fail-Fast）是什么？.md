
在`java.util`包下的大部分集合（如 `ArrayList`, `HashMap`）是不支持线程安全的，为了能够提前发现并发操作导致线程安全风险有了快速失败。

快速失败（Fail-Fast）实现原理：==集合内部维护一个 modCount 计数器，每次**结构性修改**时递增。迭代器在遍历时会检查 modCount 是否与预期值一致，不一致则抛出异常。==

Fail-Fast 不是**线程安全机制**，只是在出现问题的时候抛异常。