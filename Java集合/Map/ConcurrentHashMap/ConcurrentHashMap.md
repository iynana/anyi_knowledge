+ JDK 1.7 实现（分段锁）：内部使用 Segment 数组，每个 Segment 继承相当于一个独立的哈希表。不同线程操作不同段时互不干扰，同一段内才需要竞争锁，从而提高了并发度。
+ JDK 1.8 实现（Node + CAS\[桶为空\] + \[synchronized\]）：数组+链表/红黑二叉树，将分段锁变成粒度更小的桶级锁。（==JDK 1.7 最大并发度是 Segment 的个数，默认是 16。JDK 1.8 最大并发度是 Node 数组的大小，并发度更大==）



