+ JVM：Java虚拟机，是Java程序运行的核心，负责将字节码解释或编译为机器码，提供内存管理、垃圾回收等运行时环境。
+ JRE：Java运行时环境，包含JVM和Java核心类库，用于运行Java程序，但不能用于开发。
+ JDK：Java开发工具包，包含JRE以及编译、调试、文档生成工具等。
关系：JDK包含JRE，JRE包含JVM。JDK用于**开发**，JRE用于**运行**，JVM是**运行的核心**。