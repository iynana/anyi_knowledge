Java 泛型是 JDK5 中引入的一个新特性，允许在定义类、接口或方法时使用**类型参数**，声明的类型参数在编译时用具体的类型来替换。

**优势**：
+ **类型安全**：泛型在编译时检查类型匹配。避免了运行时类型转换错误
+ **代码复用**： 泛型允许我们为多种数据类型编写同一套逻辑，提高代码的复用性

**类型擦除**：声明的类型参数在编译时用具体的类型来替换，类型擦除使得Java泛型代码能够与Java之前版本的代码==兼容==。