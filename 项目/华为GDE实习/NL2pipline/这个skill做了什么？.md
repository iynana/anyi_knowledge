NL2Pipeline 就是把用户的自然语言数据处理需求，自动转换成数据平台原生、可编辑、可执行的 Pipeline；在我们的 DataFactory 中，最终就是生成物理模型、数据流和调度流对应的平台 DSL。

比如用户输入：每天凌晨读取订单表，过滤掉已取消订单，按城市统计销售额，结果写入ClickHouse；失败后重试三次。NL2Pipeline 会将其转换为平台原生流程。

传统情况下，用户需要在 DataFactory 中手动拖拽节点、配置参数和连接关系；NL2Pipeline 则允许用户直接输入。