+ 普通校验错误，加入错误收集器，继续向下执行：字段非法、字段类型不正确
+ 强依赖错误，快速失败(整个请求退出，部分校验退出)，避免级联错误：表不存在，列就没有必要进行校验了


调用链深，校验点分散，校验依赖复杂体现在哪里：
① 调用链深：核心接口平均 7 层，最深 9 层
5 个核心接口调用链均 ≥5 层，平均深度 7 层，最深达到 9 层。而普通 AI 分析通常在 4~5 层提前截止，因此会系统性遗漏深层业务逻辑和异常处理节点。

② 校验点分散：169 个异常点分布在 20 个文件中
共 169 个异常/校验点，分散在 20 个文件中；单文件最多 43 个异常点，最大跨越 1416 行代码。

③ 校验依赖复杂：同时存在数据依赖、控制依赖
最长存在 9 步级联校验链，主校验链最长包含 35 步连续，同时存在 6 类查库数据依赖。数据依赖解决**后续校验有没有数据可以用**，控制依赖解决**后续校验或业务逻辑还应不应该执行**。