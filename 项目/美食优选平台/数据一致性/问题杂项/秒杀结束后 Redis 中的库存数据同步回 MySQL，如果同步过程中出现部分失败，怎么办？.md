+ 幂等性保证
+ 全量对账
+ 多级重试topic
	+ 重试机制：失败操作进入重试队列，间隔指数增长
	+ 死信处理：多次重试失败后，进入死信队列，人工干预
		+ `voucher.main`
		- `voucher.retry.5s`
		- `voucher.retry.30s`
		- `voucher.retry.5m`
		- `voucher.dlq`


记录状态、幂等重试，对账补偿、人工兜底

**生产者发送不丢失**：
+ ack=-1，分区收到消息后，且同步到副本分区后，才返回响应
+ retries=3，添加重试机制
+ 多次重试失败后，进入死信队列人工干预

**Broker保存消息不丢失**：
+ 设置分区副本数量大于1

**消费者不能少消费**：
+ 手动提交offset

**支撑**：
+ 保证幂等性
+ 定时对账补偿

