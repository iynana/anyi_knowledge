- **数据不一致：**  
    主节点扣减库存后，从节点可能因复制延迟未更新，导致读取到旧库存（超卖）。
- **故障切换风险：**  
    主节点宕机，未同步到从节点的数据可能丢失，导致库存回滚。
- **解决方案：**  
    a. 秒杀场景建议使用 Redis **单节点**或**集群模式**，并开启**持久化**。  
    b. 若必须用主从，可强制读主节点（但增加主节点压力）。