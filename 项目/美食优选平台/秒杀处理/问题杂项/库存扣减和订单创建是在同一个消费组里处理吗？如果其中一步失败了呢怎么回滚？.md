**库存扣减：** 在秒杀请求入口的 Redis 层完成，同步返回用户结果。（hash存储）
**订单创建：** 通过 MQ 异步处理，用户无需等待。

**两阶段库存扣减**：
+ 预扣减阶段：redis中扣减库存，打标记
+ 确认or回滚
	+ 订单创建成功->发送确认消息，MySQL正式扣减，redis更改状态
	+ 订单创建失败->发送回滚消息，MySQL不变，redis归还冻结库存
+ 配合幂等重试、对账补偿等机制保证最终一致性