# ![[Pasted image 20260330120358.png]]

==可靠性，吞吐量==
			RabbitMQ                             RocketMQ                        Kafka
优先级队列          支持                                        支持                             不支持
延迟队列            支持(死信交换机实现)           支持延时队列                    不支持
死信队列            支持                                         支持                              不支持
重试                   支持                                     支持重试队列            生产者支持，消费者不支持     
消费模式            推模式                                    推+拉                             拉模式
事务消息             支持                                       支持                               不支持

==重试，消费模式==

## 技术选型
1. **性能和吞吐量**
2. **可靠性**
3. **消费方式**
4. **开发部署复杂度**
5. **支持的功能队列**
6. **开发部署复杂度**


为什么选择Kafka？
+ 高吞吐
+ 高可用（ISR机制）

Kafka 适合交易场景吗？
不完全适合。我的语义是"至少消费一次"，会出现重复消费

为什么后面没有把 Kafka 换掉？
吞吐量高，重复消费的问题可以通过幂等的设计解决（唯一业务id）