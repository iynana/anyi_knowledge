一部分是**流程状态**，表示当前在理解意图、等待澄清还是生成推荐；
另一部分是**业务上下文**，包含已经确认的槽位、用户约束，以及当前等待用户回答的问题。

业务上下文：每轮请求进来后，编排层读取当前会话快照，再把必要的上下文交给意图识别和槽位抽取单元。
流程状态：之后，intent agent进行意图识别。编排层根据意图识别的结果，路由到推荐(进行槽位合并，完善业务上下文)、澄清、切换、规划等链路上
