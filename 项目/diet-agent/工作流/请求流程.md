1.**会话加载**：会话级锁，加载sessionstate，其中包括历史槽位，历史推荐ID，会话阶段
2.**开启trace**，记录
3.**意图识别与槽位抽取**：经过intent agent的进行意图识别与槽位抽取
4.**兜底矫正**：规则修正与安全兜底fallback
5.**意图路由**：
+ 推荐或者澄清:==槽位合并==和澄清判断，进行检索，先从数据库召回50个候选，然后从七维槽位匹配度选取top10，最后把排名靠前的三个交给recommend agent
+ 换一批:排除历史推荐id，重新检索
+ 多餐规划:按照餐次分别推荐
+ 健康风险:返回保守提示
+ 其他:返回固定引导文案
6.检查最终回复，不合安全规范，替换为保守提示。保存最新槽位，历史推荐ID和对话消息。trace写入数据库。