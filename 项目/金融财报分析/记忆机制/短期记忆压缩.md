1. 当新对话加入后，短期记忆的 Token 数 超过预设阈值（设定的 32K Token）
2. 此时就触发摘要压缩，保留最近3轮对话确保聚焦当前任务，将其他对话进行摘要压缩，再将「摘要 + 窗口内最新对话」作为新的短期记忆存储

靠prompt约束大模型生成摘要：
```
请你对以下AI助手和用户的对话历史进行摘要，要求：
1.  简洁明了，控制在100-200字，只保留核心信息：用户的需求、已执行的操作、关键结果；
2.  不要遗漏重要参数（如用户提供的手机号、预约信息、查询的商家名称）；
3.  语言口语化，符合对话上下文逻辑；
4.  不要添加额外内容，只基于提供的对话生成摘要。

对话历史：
{window_out_dialog_text}
```
